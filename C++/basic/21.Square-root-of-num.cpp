#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int num;
    cout << " Enter the number : ";
    cin >> num;
    cout << "Square root of " << num << " is " << (float)sqrt(num) << endl;
    return 0;
}